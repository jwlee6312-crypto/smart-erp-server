<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- 💡 라이트 테마 강제 적용 및 웹 스타일 브랜딩 컬러 설정 -->
    <style name="Theme.SmartERP" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/smart_blue</item>
        <item name="colorPrimaryDark">@color/smart_blue</item>
        <item name="colorAccent">@color/core_amber</item>

        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/login_bg_end</item>

        <!-- Force Light Mode: 다크모드 시스템 설정 무시 -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>
</resources>