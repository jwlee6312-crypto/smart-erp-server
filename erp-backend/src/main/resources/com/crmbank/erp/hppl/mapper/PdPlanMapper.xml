<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crmbank.erp.hppl.mapper.PdPlanMapper">

  <!-- 주간생산계획 대상 조회 (구분별 필터 적용) -->
  <select id="getPdPlanTargetList" parameterType="map" resultType="com.crmbank.erp.hppl.dto.PdPlanDto">
    <if test="gubun == '200' or gubun == '100'">
    /* 주문 건 조회 (양산계획 시 포함) */
    select '100' as gubun, '100' as sordgbn, '주문' as sordgbnnm,
           convert(varchar(8), a.ordymd, 112) as ordymd, a.ordym, a.ordno, b.orowno,
           a.custcd, c.custnm as custnm, b.itemcd, f.itemnm, f.itsize,
           b.ordqty, b.ordqty as planqty, b.ordamt as suppamt,
           convert(varchar(8), a.outymd, 112) as napgiymd, convert(varchar(8), a.outymd, 112) as dlvhopedt, a.updemp
      from HSIO030T_TBL a
           join hsio031t_tbl b on a.cmpycd = b.cmpycd and a.ordymd = b.ordymd and a.ordno = b.ordno
           left join haba020t_tbl c on a.cmpycd = c.cmpycd and a.custcd = c.custcd
           left join hsba010t_tbl f on b.cmpycd = f.cmpycd and b.itemcd = f.itemcd
           left join (select cmpycd, ordym, ordno, orowno, sum(planqty) as planqty
                        from hppl160t_tbl
                       group by cmpycd, ordym, ordno, orowno) g
             on b.cmpycd = g.cmpycd and b.ordym = g.ordym and b.ordno = g.ordno and b.orowno = g.orowno
     where a.cmpycd = #{cmpycd}
       and a.outymd between #{frymd} and #{toymd}
       and a.sts = 'y' and f.astkind = '200'
       and isnull(b.ordqty, 0) - isnull(g.planqty, 0) > 0
    UNION ALL
    </if>
    /* 양산/기타/외주 요청 건 조회 */
      select a.gubun as gubun,
              a.gubun as sordgbn,
              case a.gubun when '200' then '양산' when '300' then '외주' else '기타' end as sordgbnnm,
              a.reqymd as ordymd, a.reqym as ordym, a.reqno as ordno, '000' as orowno,
              a.custcd, cust.custnm as custnm, a.itemcd, c.itemnm, c.itsize,
              a.planqty as ordqty, isnull(a.planqty, 0) - isnull(b.planqty, 0) as planqty, 0 as suppamt,
              a.napgiymd, a.napgiymd as dlvhopedt, a.updemp
         from hppl150t_tbl a
              left join (select cmpycd, ordym, ordno, sum(planqty) as planqty
                           from hppl160t_tbl
                          group by cmpycd, ordym, ordno) b
                on a.cmpycd = b.cmpycd and a.reqym = b.ordym and a.reqno = b.ordno
              left join hsba010t_tbl c on a.cmpycd = c.cmpycd and a.itemcd = c.itemcd
              left join haba020t_tbl cust on a.cmpycd = cust.cmpycd and a.custcd = cust.custcd
        where a.cmpycd = #{cmpycd}
          and a.reqymd between #{frymd} and #{toymd}
          and a.gubun = #{gubun}
          and isnull(a.planqty, 0) - isnull(b.planqty, 0) > 0
  </select>

  <!-- 양산/외주 요청 자료 조회 (전체 내역) -->

  <!-- 확정 목록 조회 (납기일자 기준) -->
  <select id="getPdPlanList" parameterType="map" resultType="com.crmbank.erp.hppl.dto.PdPlanDto">
      select a.yymmdd, a.ser,
             case datepart(dw, convert(date, a.yymmdd))
                when 1 then '일' when 2 then '월' when 3 then '화'
                when 4 then '수' when 5 then '목' when 6 then '금' when 7 then '토'
             end as day_nm,
             a.itemcd, b.itemnm, b.itsize, a.custcd, c.custnm as custnm,
             a.linecd, d.linenm, a.progcd, e.prognm, a.planqty, a.ordqty, a.napgiymd, a.napgiymd as dlvhopedt,
             a.ordym, a.ordno, a.orowno,
             isnull(f.gadrate, 0) as gadrate,
             isnull(f.jungrate, 0) as jungrate,
             /* 🚀 capahh와 stdworkhh 중 있는 값을 우선 사용 */
             case when isnull(f.capahh, 0) > 0 then f.capahh else isnull(f.stdworkhh, 0) end * 60 as capahh,
             isnull(f.gadtmdd, 0) as gadtmdd,
             isnull(f.pqtytt, 0) as pqtytt,
             isnull(f.pqtydd, 0) as unit_capa
        from hppl160t_tbl a
        left join hsba010t_tbl b on a.cmpycd = b.cmpycd and a.itemcd = b.itemcd
        left join haba020t_tbl c on a.cmpycd = c.cmpycd and a.custcd = c.custcd
        left join hpba130t_tbl d on a.cmpycd = d.cmpycd and a.linecd  = d.linecd
        left join hpba100t_tbl e on a.cmpycd = e.cmpycd and a.linecd  = e.linecd and a.progcd = e.progcd
        left join hpba200t_tbl f on a.cmpycd = f.cmpycd and a.itemcd = f.itemcd and a.linecd = f.linecd and a.progcd = f.progcd
       where a.cmpycd = #{cmpycd}
         and a.napgiymd between #{frymd} and #{toymd}
       order by a.napgiymd, a.yymmdd, a.linecd, a.progcd, a.ser
  </select>

  <!-- 월간 생산계획 조정용 조회 (계획일 기준) -->
  <select id="getPdPlanMonthlyList" parameterType="map" resultType="com.crmbank.erp.hppl.dto.PdPlanDto">
    select a.yymmdd, a.ser, a.gubun,
           case datepart(dw, convert(date, a.yymmdd))
              when 1 then '일' when 2 then '월' when 3 then '화'
              when 4 then '수' when 5 then '목' when 6 then '금' when 7 then '토'
           end as day_nm,
           a.itemcd, b.itemnm, b.itsize, a.custcd, c.custnm as custnm,
           a.linecd, d.linenm, a.progcd, e.prognm, a.planqty, a.ordqty, a.napgiymd,
           a.ordym, a.ordno, a.orowno,
           isnull(f.gadrate, 0) as gadrate,   /* 가동율 */
           isnull(f.jungrate, 0) as jungrate, /* 양품율 */
           /* 🚀 capahh와 stdworkhh 중 있는 값을 우선 사용 */
           case when isnull(f.capahh, 0) > 0 then f.capahh else isnull(f.stdworkhh, 0) end * 60 as capahh,
           isnull(f.gadtmdd, 0) as gadtmdd,   /* 가동시간 */
           isnull(f.pqtytt, 0) as pqtytt,     /* 일생산량 */
           isnull(f.pqtydd, 0) as unit_capa   /* 일생산능력 */
      from hppl160t_tbl a
           left join hsba010t_tbl b on a.cmpycd = b.cmpycd and a.itemcd = b.itemcd
           left join haba020t_tbl c on a.cmpycd = c.cmpycd and a.custcd = c.custcd
           left join hpba130t_tbl d on a.cmpycd = d.cmpycd and a.linecd  = d.linecd
           left join hpba100t_tbl e on a.cmpycd = e.cmpycd and a.linecd  = e.linecd and a.progcd = e.progcd
           left join hpba200t_tbl f on a.cmpycd = f.cmpycd and a.itemcd = f.itemcd and a.linecd = f.linecd and a.progcd = f.progcd
     where a.cmpycd = #{cmpycd}
       and a.yymmdd between #{frymd} and #{toymd}
       <if test="linecd != null and linecd != ''">
       and a.linecd = #{linecd}
       </if>
     order by a.yymmdd, a.linecd, a.progcd, a.ser
  </select>

  <!-- 개별 계획 조회 (이동 전 데이터 복사용) -->
  <select id="selectPdPlan" parameterType="map" resultType="com.crmbank.erp.hppl.dto.PdPlanDto">
      select a.cmpycd, a.yymmdd, a.ser, a.gubun, a.ordym, a.ordno, a.orowno,
             a.custcd, a.itemcd, a.linecd, a.progcd, a.ordqty, a.planqty, a.napgiymd, a.updemp
        from hppl160t_tbl a
       where a.cmpycd = #{cmpycd}
         and a.yymmdd = #{oldyymmdd}
         and a.ser    = #{oldser}
  </select>

  <!-- 등록자료 존재 확인 -->
  <select id="selectPdPlanCount" parameterType="map" resultType="int">
    select count(*) from HPPL160T_TBL
     where cmpycd = #{cmpycd}
       and ordym = isnull(#{ordym}, left(#{ordymd}, 6))
       and ordno  = #{ordno}
       and orowno = #{orowno}
  </select>

  <!-- 휴일 정보 조회 -->
  <select id="selectHolidayInfo" parameterType="map" resultType="map">
    select holi_dt, gubun 
      from pd_holiday
     where cmpycd = #{cmpycd} 
       and holi_dt = #{yymmdd}
  </select>

  <!-- 품목/라인별 BOM 및 공정 정보 통합 조회 -->
  <select id="selectBomAndRouting" parameterType="map" resultType="map">
    select a.linecd, a.progcd, a.itemcd, a.mitemcd, a.inqty, a.losrate
      from hpba210t_tbl a
     where a.cmpycd = #{cmpycd}
       and a.itemcd = #{itemcd}
       and a.linecd = #{linecd}
     order by a.progcd asc
  </select>

  <select id="selectNextSer" parameterType="map" resultType="string">
      select dbo.lpad(isnull(max(dbo.lpad(ser,3,'0'))+1,1),3,'0')
      from hppl160t_tbl
      where cmpycd = #{cmpycd} and yymmdd = #{yymmdd}
  </select>

  <!-- 계획 저장 (제품별 표준 공정 자동 전계) -->
  <insert id="insertPdPlan" parameterType="com.crmbank.erp.hppl.dto.PdPlanDto">
    <selectKey keyProperty="ser" resultType="string" order="BEFORE">
        select dbo.lpad(isnull(max(dbo.lpad(ser,3,'0')),0)+1,3,'0')
          from hppl160t_tbl
         where cmpycd = #{cmpycd} and yymmdd = #{yymmdd}
    </selectKey>
    insert into hppl160t_tbl (
        cmpycd, yymmdd, ser, gubun, ordym, ordno, orowno, custcd, itemcd, linecd, progcd,
        ordqty, planqty, napgiymd, addtime, updtime, updemp
    )
    select #{cmpycd}, #{yymmdd},
           dbo.lpad(cast(#{ser} as int) + row_number() over (order by b.progord, b.progcd) - 1, 3, '0'),
           #{gubun},
           isnull(#{ordym}, left(#{ordymd}, 6)), #{ordno}, #{orowno},
           #{custcd}, #{itemcd}, #{linecd}, b.progcd,
           0, #{planqty}, #{napgiymd}, getdate(), getdate(), #{updemp}
      from HPBA200T_TBL b
     where b.cmpycd = #{cmpycd}
       and b.itemcd = #{itemcd}
       and b.linecd = #{linecd}
  </insert>

  <!-- 개별 계획 단건 저장 (이동용) -->
  <insert id="insertPdPlanSingle" parameterType="com.crmbank.erp.hppl.dto.PdPlanDto">
    insert into hppl160t_tbl (
        cmpycd, yymmdd, ser, gubun, ordym, ordno, orowno, custcd, itemcd, linecd, progcd,
        ordqty, planqty, napgiymd, addtime, updtime, updemp
    ) values (
        #{cmpycd}, #{yymmdd}, #{ser}, #{gubun},
        isnull(#{ordym}, left(#{ordymd}, 6)), #{ordno}, #{orowno},
        #{custcd}, #{itemcd}, #{linecd}, #{progcd},
        #{ordqty}, #{planqty}, #{napgiymd}, getdate(), getdate(), #{updemp}
    )
  </insert>

  <update id="updatePdPlan" parameterType="com.crmbank.erp.hppl.dto.PdPlanDto">
    update hppl160t_tbl
       set itemcd = #{itemcd},
           custcd = #{custcd},
           linecd = #{linecd},
           progcd = #{progcd},
           planqty = #{planqty},
           napgiymd = #{napgiymd},
           updtime = getdate(),
           updemp = #{updemp}
     where cmpycd = #{cmpycd}
       and yymmdd = #{yymmdd}
       and ser = #{ser}
  </update>

  <select id="selectOrdQty" resultType="decimal">
    select isnull(ordqty, 0) from hppl160t_tbl
     where cmpycd = #{cmpycd} and yymmdd = #{yymmdd} and ser = #{ser}
  </select>

  <delete id="deletePdPlan">
    delete from hppl160t_tbl where cmpycd = #{cmpycd} and yymmdd = #{yymmdd} and ser = #{ser}
  </delete>

  <!-- 월간 날짜별 Capa 및 계획 요약 조회 -->
  <select id="getPdPlanMonthlySummary" parameterType="map" resultType="map">
    <![CDATA[
    with date_range as (
        select convert(varchar(8), dateadd(day, number, convert(date, #{frymd})), 112) as yymmdd
        from master..spt_values
        where type = 'p'
          and number <= datediff(day, convert(date, #{frymd}), convert(date, #{toymd}))
    ),
    plan_sum as (
        select yymmdd, sum(planqty) as total_plan
        from hppl160t_tbl
        where cmpycd = #{cmpycd} and linecd = #{linecd}
          and yymmdd between #{frymd} and #{toymd}
        group by yymmdd
    ),
    capa_sum as (
        -- 현재 계획된 품목들의 표준 Capa(pqtydd) 합계
        select a.yymmdd, sum(isnull(b.pqtydd, 0)) as total_capa
        from hppl160t_tbl a
        join hpba200t_tbl b on a.cmpycd = b.cmpycd and a.itemcd = b.itemcd and a.linecd = b.linecd and a.progcd = b.progcd
        where a.cmpycd = #{cmpycd} and a.linecd = #{linecd}
          and a.yymmdd between #{frymd} and #{toymd}
        group by a.yymmdd
    )
    select d.yymmdd,
           isnull(p.total_plan, 0) as plan_qty,
           isnull(c.total_capa, 0) as capa_qty,
           case when isnull(c.total_capa, 0) = 0 then 0
                else round((isnull(p.total_plan, 0) / isnull(c.total_capa, 0)) * 100, 1) end as rate
      from date_range d
      left join plan_sum p on d.yymmdd = p.yymmdd
      left join capa_sum c on d.yymmdd = c.yymmdd
     order by d.yymmdd
    ]]>
  </select>

</mapper>
